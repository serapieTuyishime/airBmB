/** @type {import('tailwindcss').Config} */
module.exports = {
  content: ["./index.html"],
  theme: {
    extend: {},
  },
  variants: {
    extend: {
      // put what you want to enable but won't overwrite the existing settings
    }
  },
  plugins: [],
}
